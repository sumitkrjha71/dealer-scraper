# URL of the deployed backend API (Railway / Render / self-hosted)
# Local dev: backend runs on port 3001, frontend on 3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Production example:
# NEXT_PUBLIC_API_URL=https://dealer-scraper-api.railway.app
